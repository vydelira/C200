#BookStore
class BookStore:
    def __init__(self, bookList, coffeeShop):
        self.bookList = bookList
        self.coffeeShop = coffeeShop
    def bookSearch(Title):
        for i in range(len(bookList)):
            if Title == input:
                return Title
            else:
                return "Not Found"
    def authorSearch():
        for i in range(len()):
    def coffeeSearch():
    def coffeeSale():

 

#Book
class Book:
    def __init__(self, Title, firstName, lastName, price, quantity):
        self.Title = Title
        self.firstName = firstName
        self.lastName = lastName
        self.price = float(price)
        self.quantity = int(quantity)
    def getTitle():
        return self.Title
    def getFirstName():
        return self.firstName
    def getLastName():
        return self.lastName
    def getPrice():
        return self.price
    def getQuantity():
        return self.quantity
    def inStock():  
        if self.quantity > 0:
            return True
        else:
            return False
    def sold():
        if self.quantity > 0:
            return self.quantity -= 1
    def toString(): 
        return "Book: " + str(self.Title) + ", "
        return "Author: " + str(self.firstName) + " " + self.lastName
        return "Price: " + int(price)
    def __repr__(self):
        #return self.toString

#CoffeeShop
class CoffeeShop:
    def __init_(self, coffeeList):
        self. coffeList = coffeeList
    def coffeeSearch(self):
    def coffeeSale(self):

#Coffee
class Coffee(self, drink, roast, price, quantity):
    self.drink = drink
    self.roast = roast
    self.price = float(price)
    self.quantity = int(quantity)

